// todo: add more fields to context based on different event types
export interface TStreamOnlineEventContext {
  streamer_name: string;
  streamer_login: string;
  streamer_avatar: string;
  stream_title: string;
  started_at: string;
};
